/**
 * ./src/core/utils/defineProp.js
 *
 * @flow
 */

// import { AppEventsLogger } from "trivia-logger";

export default class TriviaAnalytics {
  static logEvent(name: string, value: mixed, opts: ?{}): void {
    // AppEventsLogger.logEvent(name, value, opts);
  }
}
