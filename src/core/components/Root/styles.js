/**
 * ./src/core/components/Root/styles.js
 *
 * @flow
 */

import { StyleSheet } from "react-native";
import { theme } from "../../constants";

const styles = StyleSheet.create({
  view: theme.layout.viewFs
});

export default styles;
